FROM golang:1.21
ARG aws_access_key_id=AKIAIOSFODNN7EXAMPLE
ARG password=supersecret123
ARG db_secret=mysql_root_password

HEALTHCHECK --interval=30s CMD curl -f http://localhost:8080/health -H "Authorization: Bearer secret123" || exit 1

CMD ["/app"]
